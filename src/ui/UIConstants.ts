import {TextStyleOptions} from "../export";

export const UIConstants = {
    spinButton: {
        scaleNormal: 1,
        scalePressed: 0.95,
        scaleHovered: 1.05,
    },
    textStyles: {
        title: {
            fontFamily: "Verdana",
            fontSize: 14,
            fill: "#cccccc",
            align: "center"
        } as TextStyleOptions,
        amount: {
            fontFamily: "Verdana",
            fontSize: 20,
            fontWeight: "bold",
            fill: "#ffffff",
            align: "center"
        } as TextStyleOptions
    }
};